import React from "react";
import logo from "./logo.png";

export default Header = () => (
    <div
        style={{
            textAlign: "center",
            marginTop: 10,
        }}>
        <img width='320px' src={logo} />
    </div>
);
