import React from "react";
import logo from "./logo.png";

const Header = () => (
    <div
        style={{
            textAlign: "center",
            marginTop: 10,
        }}>
        <img width='320px' src={logo} />
    </div>
);

export default Header;
