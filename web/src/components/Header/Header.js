import logo from './logo.png';

export const Header = () => (
    <div style={{
        textAlign: 'center',
        marginTop: 10
    }}>
        <img 
            width="320px"
            src={logo}/>
    </div>
);